#include<stdio.h>
int main()
{
double nu;
     scanf("%lf",&nu);
     if(nu<=0.0)
       {
            if(nu==0.0)
            
                  printf("the value is zero");
                 else
                 printf("the value is negative");
                 }
       else
                printf("the value is positive");
                return 0;
                }
